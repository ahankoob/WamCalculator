<manifest package="com.ahankoob.wamcalculator"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_app_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_app_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".NewCalculatorActivity">
        </activity>
        <activity android:name=".MasdoodCalculatorActivity">
        </activity>
        <activity android:name=".AslSoodJadvalActivity">
        </activity>
        <activity android:name=".SoodSeporde">
        </activity>
    </application>

</manifest>
